# デプロイの記録（2026-09-09 実施済み）

## 済んだこと

- Netlify のクレジットが 9/9 にリセットされ、本番デプロイが通るようになった
- `cases.html` の「2ヶ月間」（99行目）と「運用開始2ヶ月の応募関連の反響数」（112行目）を反映
- `contact.html` のフォーム件名【晴々HP】お問い合わせを反映
- HARE AI（`netlify/functions/hare.mjs`）が本番で稼働。`/api/hare` に実際に問い合わせて
  AIの本文が返ることを確認済み＝Netlify側に鍵（ANTHROPIC_API_KEY）は設定されている

手順（次回もこれでよい）:

```bash
cd ~/Desktop/晴々_HP_公開用
python3 _src/build.py
npx netlify-cli deploy --prod --dir=site --functions=netlify/functions
```

## コミットの名義について（2026-09-09 変更）

microCMS で記事を公開すると GitHub の内容からサイトが作り直される。ところが
`harebare.hashimoto@gmail.com` 名義のコミットは **GitHub 上のどのアカウントとも
結びついていない**ため、Netlify が「知らない人のコミット」としてビルドを拒否していた
（Build blocked: Unrecognized Git contributor）。

GitHub が `monodukurilife-creator` として認識するのは `monodukurilife@gmail.com` 名義
だけなので、このフォルダのコミット名義をそちらへ変更した（このフォルダの中だけの設定）。

```bash
git config user.email monodukurilife@gmail.com
git config user.name monodukurilife-creator
```

## デプロイ先の取り違えに注意

`~/.netlify/state.json`（ホーム直下・全フォルダ共通の予備）に
**eightworks-hp-preview の siteId** が入っている。フォルダ側にリンクが無いと
そちらへ公開されてしまう。

このフォルダには `.netlify/state.json` を作って晴々（haretobare /
`4597c521-b897-4706-aff4-27bd4029a262`）に固定済み。**このファイルは消さないこと。**

（2026-09-09 追記）リポジトリを公開に切り替えた。自動ビルドが通るかの確認用。
