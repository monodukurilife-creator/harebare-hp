# microCMS 設定手順（合同会社晴々サイト）

橋本さんたちがご自分でトピックス（ブログ）を書けるようにするための、最初の1回だけの準備です。
費用はかかりません（無料枠で足ります）。

このファイルは `site/` の外に置いています。`site/` に入れるとインターネットに公開されてしまうためです。

---

## この作業で何ができるようになるか

microCMS という記事管理サービスにログインして、タイトル・日付・写真・本文を入れて
「公開」を押すと、**2〜3分後に**サイトのトピックスにその記事が出ます。
橋本さんが私に依頼する必要はなくなります。

「2〜3分」は、サイトを自動で作り直している時間です。すぐには出ません。

---

## 手順1　会員登録とサービス作成

1. https://microcms.io/ を開いて「無料ではじめる」
2. メールアドレスとパスワードで登録（Googleアカウントでも可）
3. 「サービス」を作る画面で、以下を入れる

   - サービス名 … `合同会社晴々`
   - サービスID … `haretobare`

   **サービスIDは後から変えられません。** サイト側の設定に書き込む値です。

---

## 手順2　記事の入れものを作る

1. 左のメニューから「API」→「APIを作成」→「自分で作成する」
2. 以下のとおり入れる

   - API名 … `トピックス`
   - エンドポイント … `topics`　←**この綴りは変えないでください**
   - 型 … **リスト形式**

3. 「APIスキーマを定義」で、下の表のとおりに6つ作る

| フィールドID | 表示名 | 種類 | 必須 |
|---|---|---|---|
| `title` | タイトル | テキストフィールド | ✓ |
| `date` | 公開日 | 日時 | ✓ |
| `category` | カテゴリ | セレクトフィールド | ✓ |
| `thumbnail` | 一覧に出る写真 | 画像 | ✓ |
| `excerpt` | 一覧に出る短い説明 | テキストエリア | ✓ |
| `body` | 本文 | リッチエディタ | ✓ |

**フィールドIDの綴りは1文字も変えないでください。** サイト側がこの名前で記事を探します。
表示名（2列目）は画面に出る日本語なので、分かりやすければ変えて構いません。

`category` の選択肢には、次を入れてください（あとから増やせます）。

```
AI活用事例
SNS運用のコツ
お知らせ
```

---

## 手順3　APIキーを2つ用意する

「サービス設定」→「APIキー」を開きます。

1. **読み取り専用キー**（サイトが記事を読むためのもの）
   - 既定のキーの権限を確認し、`GET` だけにチェックが入っている状態にする
   - このキーの値をメモする

2. **書き込みできるキー**（既存2記事の移行に一度だけ使う）
   - 「APIキーを作成」→ `topics` に対して `GET` `POST` にチェック
   - 移行が終わったら削除して構いません

---

## 手順4　サイト側に鍵を渡す

`~/Desktop/晴々_HP_公開用/.env.example` を `.env` という名前でコピーして、
`MICROCMS_API_KEY` に読み取り専用キーを貼り付けます。
`.env` はGitHubには上がらない設定になっています。

Netlify 側にも同じ2つを登録します（GitHub連携のときに一緒に行います）。

- `MICROCMS_SERVICE_ID` = `haretobare`
- `MICROCMS_API_KEY` = 読み取り専用キー

---

## 手順5　既存2記事の移行

書き込みできるキーを使って、今サイトに載っている2記事を microCMS へ流し込みます。
この作業は私（Claude）が行います。写真も一緒に移します。

移行が終わったら、microCMS の画面に2記事が並んでいることを確認してください。

---

## 手順6　GitHub連携と自動反映

別途ご案内します。ここまで終われば、あとは
「microCMS で公開 → Netlify が自動で作り直す」がつながります。
